import { AppstrikeThroughDirective } from './appstrike-through.directive';

describe('AppstrikeThroughDirective', () => {
  it('should create an instance', () => {
    const directive = new AppstrikeThroughDirective();
    expect(directive).toBeTruthy();
  });
});
